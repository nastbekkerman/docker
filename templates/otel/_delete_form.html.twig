<form method="post" action="{{ path('otel_delete', {'id': otel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ otel.id) }}">
    <button class="btn">Delete</button>
</form>
