image: atlassian/default-image:2

pipelines:
  default:
    - step:
        name: 'Build and Test'
        image: composer/composer:1.10.20
        script:
          - /usr/bin/composer install
#          - vendor/bin/phpcs ./src
#          - phpcs ./src

  branches:
    master:
       - step:
           name: 'To prod'
           image: composer/composer:1.10.20
           script:
             - /usr/bin/composer install
             - ssh nastbekkerman@165.232.130.229 to_prod.sh